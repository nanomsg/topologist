server:
  config-addr: !Req ipc://.test-config.sock
  updates-addr: !Pub ipc://.test-updates.sock

layouts:
  simple:
    server <- client:
      port: 10000

topologies:
  simple: !Topology
    type: reqrep
    layout: simple
    assign:
      server: 10.254.0.1
